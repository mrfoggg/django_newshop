from django.test import TestCase
from .models import Attribute, Category, Group, Product

# Create your tests here.
class CreateCatalog(TestCase):
    def setUp(self) -> None:

        return super().setUp()
